#include <stdio.h>

int main() {

    int age = 17;
    int *p = age;      // pointer

    char name[3] = "kim";
    // char *name = name[3];

    printf("%d", age);
    printf("\n%d\n", p);        // pointer : a's address

    // 1) malloc    2) String functions     // Sat. Assignment review / malloc / String function
    // Next step :  1) Algorithm (sorting / searching)
    //              2) Data Structure (Link list / Tree / Stack / Heap) C complete
    // Project Big program //
    // Racket // 

    return 0;
}